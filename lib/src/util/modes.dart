enum AlignMode {
  start,
  center,
  end,
}

enum SelectedMode {
  multiple,
  single,
}

// https://flutter.dev/docs/development/ui/advanced/gestures
enum GestureMode {
  tapDown,
  tapUp,
  tap,
  tapCancel,

  doubleTap,

  longPress,

  dragStart,
  dragUpdate,
  dragEnd,

  verticalDragStart,
  verticalDragUpdate,
  verticalDragEnd,

  horizontalDragStart,
  horizontalDragUpdate,
  horizontalDragEnd,

  panStart,
  panUpdate,
  panEnd,
}
